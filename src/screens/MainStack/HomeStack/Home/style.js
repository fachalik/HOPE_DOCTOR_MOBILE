import {StyleSheet} from 'react-native';
import {
  colors,
  fonts,
  padding,
  dimensions,
  widthPercent,
} from '../../../../styles/base';

const panelStyles = StyleSheet.create({});

export default panelStyles;
